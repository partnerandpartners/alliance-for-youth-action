extends layout

block content
  .container
    .row
      .col-xs-12.col-md-7
        h1= site.options.campaignsPageHeadline
        .lead= site.options.campaignsPageDescription
  .container.p-xs-y-3
    .row
      if posts
        each post in posts
          include ./partials/campaign-listing
