.col-xs-12.col-md-6
  article.campaign
    if !post.thumbnail
      .placeholder
    else
      img(src=post.thumbnail.url)
    h4.title #[a(href=post.permalink)= post.title]
    .excerpt!= post.excerpt
